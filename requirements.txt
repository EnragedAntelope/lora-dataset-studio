# torch + torchvision are installed separately by setup.bat / setup.sh so the
# right build (CUDA / CPU / macOS) is picked for your machine. If installing
# manually with an NVIDIA GPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# (torchvision is required by Qwen3-VL's processor even for still images)
gradio>=5.0
transformers>=4.57
accelerate>=1.0
google-genai>=1.0
pillow>=10.0
httpx>=0.27
typer>=0.12
pydantic>=2.7
pydantic-settings>=2.3
numpy>=1.26
pandas>=2.0
pyyaml>=6.0
huggingface-hub>=0.24   # HF dataset publishing (④) + tagger/model downloads

# Optional: only the WD tagger captioner (③) needs onnxruntime. This is the CPU
# build; on CUDA you can swap it for onnxruntime-gpu. It is lazy-imported, so the
# app runs fine without it until you pick a tagger.
onnxruntime>=1.17

# dev/test
pytest>=8.0
pytest-mock>=3.14
