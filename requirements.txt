# torch + torchvision are installed separately by setup.bat / setup.sh so the
# right build (CUDA / CPU / macOS) is picked for your machine. If installing
# manually with an NVIDIA GPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# (torchvision is required by Qwen3-VL's processor even for still images)
gradio>=5.0
transformers>=4.57
accelerate>=1.0
google-genai>=1.0
pillow>=10.0
httpx>=0.27
typer>=0.12
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0   # .env loading (studio/config.py); not a hard dep of pydantic-settings>=2.2
numpy>=1.26
pandas>=2.0
pyyaml>=6.0
huggingface-hub>=0.24   # HF dataset publishing (④) + tagger/model downloads

# NOTE: onnxruntime (the WD/e621 tagger backend, ③) is installed by
# setup.bat / setup.sh, NOT here — the CPU build (`onnxruntime`) or the CUDA
# build (`onnxruntime-gpu`) is chosen to match your PyTorch install. It is
# lazy-imported, so the app runs fine without it until you pick a tagger. To
# add it by hand: `pip install onnxruntime` (CPU) or `onnxruntime-gpu` (CUDA).

# dev/test
pytest>=8.0
pytest-mock>=3.14
ruff>=0.6            # lint (config in pyproject.toml); run in CI
tomli>=2.0 ; python_version < "3.11"   # tests parse emitted TOML; tomllib is 3.11+ stdlib
